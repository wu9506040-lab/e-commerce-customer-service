"""
Citation Formatter（Sprint 3 占位空壳）

职责（未来）：
- 规范引用标签：[1][2]... / [订单] / [退款] / [商品] / [知识库]
- 去重 / 排序 / 一致性检查
- 暴露给前端用于"知识来源" UI 渲染

当前状态：空壳（Sprint 3 不在此处落地逻辑；现有 prompt_assembler.format_*
按各 intent 临时打了标签；统一抽象留给未来 Sprint）。

依据：`docs/decisions/2026-07-12-sprint-3-synthesizer-split.md` §4 拆分边界
"""
# 当前无任何符号；模块保留为占位，确保 import 路径 `/services/chat/citation_formatter` 可用
